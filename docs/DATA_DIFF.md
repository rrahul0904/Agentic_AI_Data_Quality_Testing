# Data Diff

Data diff supports row-count, schema, key, row/join, hash, aggregate, profile and cascade strategies with bounded execution, tolerances and audit-column exclusion. The warehouse engine is connector-driven and avoids unnecessary raw-data egress where remote pushdown is available.
