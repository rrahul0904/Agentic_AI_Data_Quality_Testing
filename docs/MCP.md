# MCP

MCP configuration supports list/add/remove/enable/disable, discovery/catalog/install, authentication status, tools/resources, invocation and OAuth begin/callback with state/PKCE validation. MCP invocations cross the same governance boundary as native tools.
