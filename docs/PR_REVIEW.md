# PR Review

dbt review combines changed files, artifacts, validators, compiled SQL, lineage/impact, tests, schema/PII evidence and deterministic risk rules. The blocking verdict is deterministic; an LLM may summarize but cannot decide the gate. GitHub/GitLab posting is separately governed and may be SKIP_EXTERNAL.
