# SQL Intelligence

SQLGlot-backed deterministic analysis provides classification, review, formatting, translation, fingerprints, semantic checks, rewrites/fixes, optimization, execution safety, autocomplete and column lineage across supported dialects. Regex is not the primary parser.

Destructive statements are rejected before connector execution unless a separately governed mutation path explicitly permits them.
