# Tracing

Traces persist tool/generation/session evidence, timings, provider/model usage, errors, approvals, retries and selected runtime context. Replay defaults to recorded replay and does not re-execute side effects.
