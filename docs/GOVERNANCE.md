# Governance

Governance includes metadata-based PII classification/propagation, SQL policy checks, sensitive downstream exposure, RBAC inventory/object access and excessive privilege analysis. Secrets and raw credential values are never returned by discovery APIs.
