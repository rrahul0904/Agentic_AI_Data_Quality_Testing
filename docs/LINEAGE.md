# Lineage

Lineage combines SQL AST value lineage, dbt artifact graphs and the global platform asset graph. It supports aliases, nested CTEs/subqueries, joins, CASE/casts/functions, aggregates/windows, UNION, wildcard expansion with schema context and dbt ref/source resolution.

The lineage benchmark records precision, recall, F1, parse failures and unresolved references.
