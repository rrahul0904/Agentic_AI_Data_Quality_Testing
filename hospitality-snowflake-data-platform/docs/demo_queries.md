# Demo queries

Run queries in `dashboards/` with `HOSPITALITY_ANALYST_ROLE` and `WH_ANALYTICS`. Bind `:guest_key` in `guest_360.sql` through the chosen SQL client. The queries intentionally use only synthetic identifiers and non-PII analytical attributes.

